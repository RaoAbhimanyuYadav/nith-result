import { frontpage } from "../components/Temp_data/frontpage";

const initialData = {
  students: frontpage,
};

const reducer = (state = initialData, action) => {
  return state;
};

export default reducer;
